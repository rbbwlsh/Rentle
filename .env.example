# Production database (Neon Postgres). Set on the Netlify site, not in git:
#   netlify env:set NEON_DATABASE_URL "postgres://..."
# Then apply the schema and load the answer key:
#   NEON_DATABASE_URL=... npm run db:migrate && npm run db:seed
NEON_DATABASE_URL=
