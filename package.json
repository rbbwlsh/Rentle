{
  "name": "rentle",
  "version": "2.0.0",
  "description": "Rentle — the daily guess-the-rent game on real UK listings. Fully static: a pre-scraped corpus, a client-side game engine, and Netlify.",
  "private": true,
  "type": "module",
  "scripts": {
    "seed": "node tools/seed.js",
    "images": "node tools/images.js",
    "corpus": "node tools/build-corpus.js",
    "dev": "npm run corpus && npm --prefix client run dev",
    "build": "npm run corpus && npm --prefix client run build && node tools/prerender.js",
    "preview": "npm --prefix client run preview",
    "test": "node --test test/*.test.mjs",
    "postinstall": "npm --prefix client install"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "sharp": "^0.33.4"
  }
}
